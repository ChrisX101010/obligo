# Custom Solana RPC URL. Optional — if unset, defaults to public devnet.
# For demo deployments, public devnet works. For production-volume reads
# (Pass 2 onwards), use a paid RPC provider with adequate getMultipleAccounts limits.
NEXT_PUBLIC_SOLANA_RPC_URL=
