// Pure presentational. The grid background that sits behind everything.
// Server component; no client-side state.

export function LatticeBackground() {
  return <div className="lattice-bg" aria-hidden />;
}
